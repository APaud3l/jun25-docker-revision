services:
  app:
    build: 
      context: .
      target: production
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      MONGO_URL: mongodb://mongo:27017/testdb
    depends_on:
      - mongo
    restart: unless-stopped
  
  mongo:
    image: mongo
    volumes: 
      - mongo-data:/data/db

volumes:
  mongo-data: